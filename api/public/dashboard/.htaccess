# SPA fallback for the React admin dashboard served under /dashboard.
<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteBase /dashboard/
    RewriteCond %{REQUEST_FILENAME} -f [OR]
    RewriteCond %{REQUEST_FILENAME} -d
    RewriteRule ^ - [L]
    RewriteRule ^ index.html [L]
</IfModule>
