# SPA fallback for the customer storefront served under /store.
<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteBase /store/
    RewriteCond %{REQUEST_FILENAME} -f [OR]
    RewriteCond %{REQUEST_FILENAME} -d
    RewriteRule ^ - [L]
    RewriteRule ^ index.html [L]
</IfModule>
